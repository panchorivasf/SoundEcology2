% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diel_indices.R
\name{plot_diel_indices}
\alias{plot_diel_indices}
\title{Plot Diel Indices with Optional LOESS Smoothing and Scaling Transformations}
\usage{
plot_diel_indices(
  data,
  sensor_id = NULL,
  plot.title = "Circadian Indices",
  loess = TRUE,
  span = 0.3,
  scaling = "none"
)
}
\arguments{
\item{data}{A data frame containing \code{datetime}, \code{index}, and \code{value_avg} columns.}

\item{plot.title}{A character string specifying the title of the plot. Defaults to "Diel Index Patterns".}

\item{loess}{A logical value indicating whether to apply LOESS smoothing to the lines. Defaults to \code{TRUE}.}

\item{span}{A numeric value indicating the smoothing parameter for LOESS. Defaults to 0.3.}

\item{scaling}{A character string specifying the type of scaling/transformation to apply.
Options are "none" (default), "minmax", "maxabs", "zscore", "log", and "robust".}

\item{sensor.id}{Character. Sensor identifier as expressed in the 'sensor_id' column. If not NULL, only the data for the requester sensor will be plotted.}
}
\value{
A \code{plotly} object showing a time series of scaled indices for each index group.
}
\description{
This function takes a dataset containing datetime and index values and creates
an interactive plot of the scaled indices over time. Optionally, LOESS smoothing
can be applied to the line plots, replacing the original lines. Users can apply
various transformations to the data for better comparison.
}
\examples{
\dontrun{
  # Assuming `data` is a data frame with the necessary columns
  plot_diel_indices(data, loess = TRUE, scaling = "log")
}
}
